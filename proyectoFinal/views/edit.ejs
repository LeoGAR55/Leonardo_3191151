<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Usuario</title>
    <link rel="stylesheet" href="/estilos.css">
</head>
<body>
    <h2>Editar Usuario</h2>
    <form action="/update/<%= usuarios.id %>" method="POST">
        <div>
            <label for="nombre">Nombre:</label>
            <input type="text" id="nombre" name="nombre" value="<%= usuarios.nombre %>" required>
        </div>
        <div>
            <label for="ap_pat">Apellido Paterno:</label>
            <input type="text" id="ap_pat" name="ap_pat" value="<%= usuarios.ap_pat %>" required>
        </div>
        <div>
            <label for="ap_mat">Apellido Materno:</label>
            <input type="text" id="ap_mat" name="ap_mat" value="<%= usuarios.ap_mat %>" required>
        </div>
        <div>
            <label for="edad">Edad:</label>
            <input type="number" id="edad" name="edad" value="<%= usuarios.edad %>" required>
        </div>
        <div>
            <label for="no_cuenta">Número de Cuenta:</label>
            <input type="text" id="no_cuenta" name="no_cuenta" value="<%= usuarios.no_cuenta %>" required>
        </div>
        <div>
            <label for="email">Email:</label>
            <input type="email" id="email" name="email" value="<%= usuarios.email %>" required>
        </div>
        <div>
            <label for="telefono">Teléfono:</label>
            <input type="text" id="telefono" name="telefono" value="<%= usuarios.telefono %>" required>
        </div>
        <div>
            <label for="carrera">Carrera:</label>
            <input type="text" id="carrera" name="carrera" value="<%= usuarios.carrera %>" required>
        </div>
        <div>
            <button type="submit">Confirmar</button>
        </div>
    </form>
</body>
</html>
